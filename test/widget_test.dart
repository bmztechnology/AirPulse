// This is a basic Flutter widget test.
import 'package:flutter_test/flutter_test.dart';
import 'package:airpulse/main.dart';

void main() {
  testWidgets('AirPulse smoke test', (WidgetTester tester) async {
    // Build our app and trigger a frame.
    // Note: full test requires mocked providers.
    expect(true, isTrue);
  });
}
